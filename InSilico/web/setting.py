# global settings for experiment?

DEG_PVAL = 0.1          # pval, used for target validation (if too low, disconnect all edge)
EDGE_LOAD_PVAL = 0.1    # pvalue thres used for loading edge database
EDGE_TARGET_PVAL = 0.1  # pvalue thres used for extracting target genes
DEG_FILTER_PVAL = 0.2   # pvalue used for normal(mid) nodes
